source "https://rubygems.org"

# Matches the GitHub Pages build environment (Jekyll 3.x + whitelisted plugins).
gem "github-pages", group: :jekyll_plugins

# Needed for `jekyll serve` on Ruby 3+.
gem "webrick"
